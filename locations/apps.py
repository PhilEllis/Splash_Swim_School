from django.apps import AppConfig


class LocationsConfig(AppConfig):
    """
    AppConfig for the Locations app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'locations'
